rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all documents if the user is authenticated
    // In production, you would have more granular rules based on roles
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
